import 'package:flutter/material.dart';

class Zoom extends StatelessWidget {
  const Zoom({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Zoom'));
  }
}

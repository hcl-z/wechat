import 'package:flutter/material.dart';

class Firends extends StatelessWidget {
  const Firends({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('firends'),
    );
  }
}